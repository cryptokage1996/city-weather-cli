🦀 city weather cli 🦀

A simple CLI tool to show weather of a city

# installation

```bash
cargo install
cargo run -- --help
cargo run -- city -c Kansas
```

# build

```
cargo build
cargo install --path .
```
